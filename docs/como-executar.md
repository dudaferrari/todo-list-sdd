# Como Executar

## Backend

1. Instale as dependências com `npm install`.
2. Inicie o servidor com `npm start`.

## Frontend

1. Entre na pasta `apps/frontend`.
2. Instale as dependências com `npm install`.
3. Inicie o ambiente com `npm run dev`.

## Testes

Execute `npm test` na raiz do projeto para validar o backend.
